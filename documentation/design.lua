--
--[[

type text, number,

resources | diagram | screenplay | translations |

text | dialog | node


linked an unlinked dialogue (edit mode)
duplicated link

tree
 projects -- add project
  --project (p) add scene
   --scene (s)  add act
     --act (a) add dialog
       --dialog (d) add dialog
           --dialog (d) add dialog
           --dialog (d) add dialog
           --dialog (d) add dialog
               --dialog (d) add dialog
           --dialog (d) add dialog
           --dialog (d) add dialog

  --game
    -- dynamic dialog

  --gamemenu
    -- new game
    -- save
    --options
       --language
          --en
          --jp
          --back link options
          --menu link menu
       --back menu
    -- exit

   --game hud

   --variables

--dialogs
 --
 --

--start nodes (node of dialog )
--randompool (nodes of dialogs)

--actors
  narator
  pc
  dummy
--scripts

--templates
--targets

--tools

group item
node tree same level group , leaf


node["node.links"] = {
    "1223",
    "3333",
}

node["node.link"] = {
    ["node-id"] = "1223",
    ["node-direction"] = "out",
    ["node-value"] = "5",
}


node["dialog.outputs"] = {
}
node["dialog.inputs"] = {
}

node.dialog1
links = 7777

node.dialog2
links = 7777

node.links777
    link1 -> direction-out_id
    link2 -> direction-in_id


node.dialog3

]]
--
